# specify language
language:
  - python

# specify python vesrion(s)
python:
  - "3.3"
  - "3.4"
  - "2.7"

# install dependencies
install:
  - pip install -r requirements.txt

# run tests
script:
  - nosetests
